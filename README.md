# MCU Layout - Управление раскладкой участников видеоконференций

Приложение на Next.js для управления раскладкой участников в видеоконференциях с функциональностью drag & drop.

## Функциональность

- 🔐 Авторизация пользователей
- 📋 Список конференций
- 👥 Управление участниками конференции
- 🎯 Drag & Drop размещение участников в ячейках раскладки
- 📱 Адаптивный интерфейс

## Технологии

- **Next.js 14** - React фреймворк
- **TypeScript** - типизация
- **Tailwind CSS** - стилизация
- **React DnD** - drag & drop функциональность
- **Axios** - HTTP клиент

## Установка

1. Установите зависимости:
```bash
npm install
```

2. Создайте файл `.env.local` в корне проекта:
```env
API_URL=https://ivcs.profcontact.by/api
```

**Важно:** Используйте `API_URL` (без `NEXT_PUBLIC_`), так как API routes работают на сервере и не должны быть доступны в браузере.

3. Запустите dev сервер:
```bash
npm run dev
```

4. Откройте [http://localhost:3000](http://localhost:3000) в браузере

## Структура проекта

```
MCU-Layout/
├── app/                    # Next.js App Router
│   ├── login/             # Страница авторизации
│   ├── conferences/       # Список конференций
│   │   └── [id]/         # Страница конференции
│   ├── layout.tsx         # Корневой layout
│   ├── page.tsx           # Главная страница
│   └── globals.css        # Глобальные стили
├── components/            # React компоненты
│   ├── ParticipantList.tsx   # Список участников
│   └── LayoutGrid.tsx        # Раскладка с ячейками
├── lib/                   # Утилиты и API
│   ├── api.ts            # API клиент
│   └── auth.ts           # Функции авторизации
└── package.json
```

## API Интеграция

Приложение интегрировано с API по адресу: https://ivcs.profcontact.by/doc/api/rest.html

### Основные эндпоинты:

- `POST /auth/login` - авторизация
- `GET /auth/me` - текущий пользователь
- `GET /conferences` - список конференций
- `GET /conferences/:id` - информация о конференции
- `GET /conferences/:id/participants` - участники конференции
- `PUT /conferences/:id/participants/:participantId/position` - обновление позиции участника
- `GET /conferences/:id/layout` - раскладка конференции
- `PUT /conferences/:id/layout` - обновление раскладки

## Использование

1. **Авторизация**: Войдите в систему используя ваши учетные данные
2. **Выбор конференции**: Выберите конференцию из списка
3. **Управление раскладкой**: 
   - Перетащите участника из списка в нужную ячейку раскладки
   - Участник будет размещен в выбранной ячейке
   - Позиция автоматически сохраняется через API

## Разработка

```bash
# Запуск dev сервера
npm run dev

# Сборка для production
npm run build

# Запуск production сервера
npm start

# Линтинг
npm run lint
```

## Лицензия

MIT

# mcu-layouts-control
