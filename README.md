# MCU Layout - Управление раскладкой участников видеоконференций

Веб-приложение на Next.js для управления раскладкой участников в видеоконференциях с функциональностью drag & drop и видео стримингом в реальном времени.

## 🎯 Основные возможности

- 🔐 **Авторизация пользователей** - вход в систему через REST API
- 📋 **Управление конференциями** - просмотр и управление активными конференциями
- 👥 **Управление участниками** - добавление, удаление и перестановка участников в раскладке
- 🎯 **Drag & Drop интерфейс** - интуитивное размещение участников в ячейках раскладки
- 📺 **Видео стриминг** - отображение смешанного потока конференции в реальном времени
- 🎨 **Гибкие раскладки** - создание и применение различных шаблонов раскладок
- 🔄 **События в реальном времени** - автоматическое обновление через WebSocket Event Channel
- 📱 **Адаптивный интерфейс** - работа на различных устройствах

## 🛠 Технологии

- **Next.js 14** - React фреймворк с App Router
- **TypeScript** - строгая типизация
- **Tailwind CSS** - утилитарная CSS библиотека
- **React DnD** - drag & drop функциональность
- **WebRTC** - видео стриминг (HTTP POST и WebSocket signalling)
- **WebSocket** - события в реальном времени через Server-Sent Events (SSE)
- **Axios** - HTTP клиент для API запросов

## 📦 Установка и запуск

### Требования

- Node.js 23.10.0+ (рекомендуется v23.10.0)
- npm или yarn
- Доступ к API серверу видеоконференций

### Быстрый старт

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd MCU-Layout
```

2. **Установите зависимости:**
```bash
npm install
```

3. **Настройте переменные окружения:**

Создайте файл `.env.local` в корне проекта:

```env
# REST API URL (с протоколом http:// или https://)
API_URL=https://your-api-domain.com/api/rest

# WebSocket host для server-side соединений (с протоколом ws:// или wss://)
WS_HOST=wss://your-ws-domain.com

# WebSocket host для клиентских WebRTC2 соединений (только домен, БЕЗ протокола)
# Протокол добавляется автоматически в зависимости от текущего протокола страницы
NEXT_PUBLIC_WS_HOST=your-ws-domain.com
```

**Важно:**
- `API_URL` - полный URL для REST API запросов
- `WS_HOST` - используется на сервере для проксирования WebSocket через SSE
- `NEXT_PUBLIC_WS_HOST` - только домен для клиентских WebRTC2 WebSocket соединений

4. **Запустите dev сервер:**
```bash
npm run dev
```

5. **Откройте в браузере:**
```
http://localhost:3000
```

## 📁 Структура проекта

```
MCU-Layout/
├── app/                          # Next.js App Router
│   ├── api/                      # API Routes (прокси к бэкенду)
│   │   ├── auth/                 # Авторизация
│   │   ├── conferences/          # Управление конференциями
│   │   ├── layouts/              # Управление раскладками
│   │   ├── media/                # WebRTC signalling proxy
│   │   └── websocket/            # WebSocket Event Channel (SSE)
│   ├── conferences/              # Страницы конференций
│   │   └── [id]/                 # Страница конференции
│   ├── layouts/                  # Управление раскладками
│   ├── login/                    # Страница авторизации
│   ├── layout.tsx                # Корневой layout
│   └── globals.css               # Глобальные стили
├── components/                   # React компоненты
│   ├── CompactVideoStream.tsx    # Компактный видео стрим
│   ├── ConferenceMixedStream.tsx # Смешанный поток конференции
│   ├── VideoStream.tsx           # WebRTC видео стрим
│   ├── LayoutGrid.tsx            # Сетка раскладки
│   ├── ParticipantList.tsx       # Список участников
│   └── WebSocketProvider.tsx     # Провайдер WebSocket
├── hooks/                        # React хуки
│   ├── useConferenceEvents.ts    # Обработка событий конференции
│   └── useConferenceData.ts      # Данные конференции
├── lib/                          # Утилиты и API
│   ├── api.ts                    # API клиент
│   ├── api-client.ts             # Базовый HTTP клиент
│   ├── auth.ts                   # Функции авторизации
│   ├── websocket.ts              # WebSocket Event Channel
│   ├── layout-utils.ts           # Утилиты для раскладок
│   ├── logger.ts                 # Система логирования
│   └── retry.ts                  # Утилиты для повторных попыток
└── public/                        # Статические файлы
    └── clients-openapi.json      # OpenAPI спецификация
```

## 🔌 API Интеграция

### Основные эндпоинты:

**Авторизация:**
- `POST /api/auth/login` - вход в систему
- `GET /api/auth/me` - текущий пользователь

**Конференции:**
- `GET /api/conferences` - список конференций
- `GET /api/conferences/:id` - информация о конференции
- `POST /api/conferences/:id/join` - присоединение к конференции
- `POST /api/conferences/:id/leave` - выход из конференции
- `GET /api/conferences/:id/participants` - участники конференции
- `GET /api/conferences/:id/media-info` - медиа информация (WebRTC URLs)
- `GET /api/conferences/:id/layout` - раскладка конференции
- `PUT /api/conferences/:id/layout` - обновление раскладки

**Раскладки:**
- `GET /api/layouts` - список шаблонов раскладок
- `GET /api/layouts/:id` - детали раскладки

**WebRTC Signalling:**
- `POST /api/media/signalling?path=...` - прокси для WebRTC signalling

**WebSocket Event Channel:**
- `GET /api/websocket/event-channel?session=...` - SSE поток событий

## 🎬 Использование

### Авторизация
1. Откройте приложение в браузере
2. Введите учетные данные (логин и пароль)
3. После успешной авторизации вы будете перенаправлены на страницу конференций

### Управление конференцией
1. Выберите конференцию из списка
2. Присоединитесь к конференции (автоматически при открытии страницы)
3. Управляйте раскладкой:
   - Перетащите участника из списка в нужную ячейку
   - Измените тип ячейки (EMPTY, AUTO, SPEAKER, CAROUSEL и т.д.)
   - Настройте номер докладчика для ячеек типа SPEAKER
   - Примените сохраненную раскладку из списка шаблонов
4. Просматривайте видео стрим:
   - Компактный стрим в правой панели
   - Фоновый стрим в области раскладки
   - Полноэкранный режим по клику

### Типы ячеек раскладки
- **EMPTY** - пустая ячейка
- **AUTO** - автоматическое размещение
- **SPEAKER** - ячейка для докладчика (с номером)
- **CAROUSEL** - карусель участников
- **PREVIOUS_SPEAKER** - предыдущий докладчик

## 🚀 Разработка

```bash
# Запуск dev сервера
npm run dev

# Сборка для production
npm run build

# Запуск production сервера
npm start

# Линтинг
npm run lint
```

## 🎥 Видео стриминг

Приложение поддерживает два типа WebRTC signalling:

1. **WebRTC (HTTP POST)** - классический метод через HTTP POST запросы
2. **WebRTC2 (WebSocket)** - современный метод через WebSocket соединения

Протокол определяется автоматически на основе URL и параметров из API.

## 🔧 Конфигурация

### Переменные окружения

| Переменная | Описание | Пример |
|-----------|----------|--------|
| `API_URL` | URL REST API | `https://api.example.com/api/rest` |
| `WS_HOST` | WebSocket host для сервера | `wss://ws.example.com` |
| `NEXT_PUBLIC_WS_HOST` | WebSocket host для клиента | `ws.example.com` |

### Next.js конфигурация

Файл `next.config.js` содержит:
- Настройки для react-dnd (динамический импорт)
- Webpack конфигурация для SSR
- Production оптимизации

## 📝 Логирование

Система логирования настроена для production:
- **Development**: все логи (DEBUG, INFO, WARN, ERROR)
- **Production**: только важные логи (INFO, WARN, ERROR)

Используйте `logger` из `@/lib/logger` для логирования в коде.

## 🐛 Отладка

Для отладки WebRTC соединений проверьте:
1. Консоль браузера (F12) - логи VideoStream компонента
2. Network tab - HTTP запросы к API
3. WebSocket соединения - в DevTools

## 📄 Лицензия

MIT

## 🔗 Дополнительная документация

Для развертывания в production см. [SETUP.md](./SETUP.md)
